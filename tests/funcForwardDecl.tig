/* MG. Use of functions not yet defined. Make sure 
   the arguments are passed in the right order. */

let 
  function div_by_two(a:int):int = div(a, 2)
  function div(a:int, b:int):int = a/b
in
  printi(div_by_two(10))
end
